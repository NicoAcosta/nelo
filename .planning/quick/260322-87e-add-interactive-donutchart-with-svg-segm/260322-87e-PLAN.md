---
quick_id: 260322-87e
slug: add-interactive-donutchart-with-svg-segm
description: add interactive DonutChart with SVG segments
date: 2026-03-22
status: pending
---

# Quick Task 260322-87e: Add Interactive DonutChart with SVG Segments

## Task

Create `src/components/estimate/donut-chart.tsx` — an interactive SVG-based radial donut chart showing construction cost categories, with hover states, color assignments, and a legend grid.

## Plan

### Task 1: Create DonutChart component

**Files:**
- `src/components/estimate/donut-chart.tsx` (create)

**Action:**
Write the `DonutChart` client component exactly as specified. The component:
- Accepts `categories: CategoryTotal[]` and `totalPrice: number`
- Filters to active categories (subtotal > 0), sorts descending
- Uses `assignCategoryColors` from `./category-colors`
- Shows top 9 categories individually, groups the rest as "+ N more"
- Renders SVG donut using `strokeDasharray`/`strokeDashoffset` technique
- Shows hover glow effect (brightness + drop-shadow filter)
- Renders a 2-column legend grid with color dot, name, and percent
- Center label shows `formatCompact(totalPrice)` and category count

**Verify:**
- File exists at `src/components/estimate/donut-chart.tsx`
- Imports resolve: `CategoryTotal` from `@/lib/estimate/types`, `assignCategoryColors` from `./category-colors`, `formatCompact`/`formatPercent` from `./format`
- Component exports `DonutChart` as named export
- No TypeScript errors

**Done:** `src/components/estimate/donut-chart.tsx` created and committed with message `feat(estimate): add interactive DonutChart with SVG segments`
