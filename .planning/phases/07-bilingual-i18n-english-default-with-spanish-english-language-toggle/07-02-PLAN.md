---
phase: 07-bilingual-i18n
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/lib/pricing/system-prompt-builder.ts
  - src/lib/pricing/categories-config.ts
  - src/lib/estimate/engine.ts
  - src/lib/ai/tools.ts
  - src/app/api/chat/route.ts
autonomous: true
requirements:
  - I18N-04
  - I18N-05
  - I18N-06

must_haves:
  truths:
    - "buildSystemPrompt accepts a locale param and returns Spanish prompt for 'es', English for 'en'"
    - "Category config has both nameEn and nameEs fields for all 26 categories"
    - "Engine assumption labels use translations from i18n"
    - "API route reads locale from request body and passes to buildSystemPrompt"
    - "Tool user-facing message strings are locale-aware"
  artifacts:
    - path: "src/lib/pricing/system-prompt-builder.ts"
      provides: "Bilingual system prompt builder"
      exports: ["buildSystemPrompt"]
    - path: "src/lib/pricing/categories-config.ts"
      provides: "Category config with en/es display names"
      contains: "nameEn"
    - path: "src/app/api/chat/route.ts"
      provides: "Locale-aware chat API"
      contains: "locale"
  key_links:
    - from: "src/app/api/chat/route.ts"
      to: "src/lib/pricing/system-prompt-builder.ts"
      via: "buildSystemPrompt(userMode, locale)"
      pattern: "buildSystemPrompt.*locale"
    - from: "src/lib/estimate/engine.ts"
      to: "src/lib/i18n/translations.ts"
      via: "import translations for assumption labels"
      pattern: "translations"
---

<objective>
Make the backend layer bilingual: system prompt in both languages, category config with en/es names, engine assumption labels locale-aware, API route accepts locale parameter.

Purpose: The chatbot must speak the user's selected language, and all backend-generated strings must match.
Output: Updated system-prompt-builder.ts, categories-config.ts, engine.ts, tools.ts, route.ts
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bilingual-i18n-english-default-with-spanish-english-language-toggle/07-01-SUMMARY.md

<interfaces>
From src/lib/i18n/types.ts (created in plan 01):
```typescript
export type Locale = 'en' | 'es';
export const DEFAULT_LOCALE: Locale = 'en';
export const LOCALE_STORAGE_KEY = 'nelo-locale';
```

From src/lib/i18n/translations.ts (created in plan 01):
```typescript
export const translations: Record<Locale, Record<string, string>>;
// Keys include: engine.assumptionCeilingHeight, engine.assumptionFootprint, etc.
// Keys include: category.trabajos_preliminares, category.procedimientos_cumplimientos, etc.
// Keys include: expressQuestion.totalFloorAreaM2, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add English system prompt and bilingual category names</name>
  <files>src/lib/pricing/system-prompt-builder.ts, src/lib/pricing/categories-config.ts, src/lib/pricing/__tests__/system-prompt-builder.test.ts</files>
  <read_first>
    - src/lib/pricing/system-prompt-builder.ts (full file)
    - src/lib/pricing/categories-config.ts (full file)
    - src/lib/estimate/types.ts
    - src/lib/i18n/types.ts
  </read_first>
  <behavior>
    - Test 1: buildSystemPrompt('consumer', 'en') returns prompt containing "You are Nelo" (English)
    - Test 2: buildSystemPrompt('consumer', 'es') returns prompt containing "Sos Nelo" (Spanish, existing behavior)
    - Test 3: buildSystemPrompt('professional', 'en') returns prompt with "PROFESSIONAL" in English
    - Test 4: every CategoryConfig has both `name` (Spanish, kept for backward compat) and `nameEn` fields
    - Test 5: EXPRESS_QUESTIONS entries have both `label` (Spanish) and `labelEn` (English)
    - Test 6: buildSystemPrompt defaults to 'en' when no locale passed
  </behavior>
  <action>
    1. Create or update `src/lib/pricing/__tests__/system-prompt-builder.test.ts` FIRST (TDD).

    2. Update `src/lib/pricing/categories-config.ts`:
       - Add `nameEn: string` field to CategoryConfig type (in types.ts if needed, or inline)
       - Add `nameEn` to ALL 26 categories. English translations:
         - trabajos_preliminares: "Preliminary Work"
         - procedimientos_cumplimientos: "Procedures & Compliance"
         - movimiento_de_suelos: "Earthwork"
         - estructura: "Structure"
         - mamposteria: "Masonry"
         - aislaciones: "Insulation & Waterproofing"
         - cubierta: "Roofing"
         - revoques: "Plaster & Rendering"
         - contrapisos_carpetas: "Subfloors & Screeds"
         - pisos: "Flooring"
         - zocalos: "Baseboards"
         - revestimientos: "Wall Cladding"
         - cielorrasos: "Ceilings"
         - carpinterias: "Doors & Windows"
         - herreria: "Metalwork"
         - vidrios: "Glazing"
         - pintura: "Painting"
         - instalacion_sanitaria: "Plumbing"
         - instalacion_gas: "Gas Installation"
         - instalacion_electrica: "Electrical"
         - calefaccion_aa: "HVAC"
         - ascensores: "Elevators"
         - equipamiento: "Equipment & Fixtures"
         - paisajismo: "Landscaping"
         - limpieza_final: "Final Cleanup"
         - varios: "Miscellaneous"
       - Add `labelEn` to all 6 EXPRESS_QUESTIONS entries:
         - "How many square meters is your project?"
         - "How many stories does the building have?"
         - "What is the main structural system?"
         - "Roof / ceiling type?"
         - "Finish level?"
         - "In which area of Buenos Aires?"

    3. Update `src/lib/pricing/system-prompt-builder.ts`:
       - Change signature to `buildSystemPrompt(userMode: UserMode = 'consumer', locale: Locale = 'en'): string`
       - Import `Locale` from `@/lib/i18n/types`
       - When locale is 'en', use English system prompt text:
         ```
         <role>
         You are Nelo, a construction cost estimation assistant for Buenos Aires (AMBA region), Argentina.
         Your goal is to collect information about the user's project and calculate an estimated budget with price per m2 and total price.
         </role>
         ```
       - Keep the existing Spanish prompt for locale 'es' (current text)
       - Use `cat.nameEn` vs `cat.name` in category list based on locale
       - Use `q.labelEn` vs `q.label` in express questions based on locale
       - Consumer mode instructions in English:
         "The user is a CONSUMER (person who wants to build their home).
         - Use simple, clear language without unnecessary technical jargon.
         - Ask the N Express mode questions, one at a time.
         - Briefly explain why you ask each question.
         - When information is missing, state the assumptions you use.
         - If the user uploads a floor plan, analyze it and present extracted data for confirmation."
       - Professional mode instructions in English (similar translation)
       - Behavior section in English:
         "- Ask ONE question at a time. Don't overwhelm the user.
         - When you have enough data (minimum: area + stories + structure + roof + finishes + zone), offer to calculate the budget.
         - If the user uploads a floor plan image, use the analyzeFloorPlan tool.
         - After extracting data from a plan, present it for confirmation before using it.
         - Always state the assumptions you make for missing fields.
         - If the user corrects a value, update it and recalculate."
  </action>
  <verify>
    <automated>cd /Users/nico/dev/arqui && npx vitest run src/lib/pricing/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "nameEn" src/lib/pricing/categories-config.ts
    - grep -q "labelEn" src/lib/pricing/categories-config.ts
    - grep -q "locale" src/lib/pricing/system-prompt-builder.ts
    - grep -q "You are Nelo" src/lib/pricing/system-prompt-builder.ts
    - grep -q "Sos Nelo" src/lib/pricing/system-prompt-builder.ts
    - grep -q "Locale" src/lib/pricing/system-prompt-builder.ts
    - All system prompt tests pass
  </acceptance_criteria>
  <done>buildSystemPrompt produces full English or Spanish prompts based on locale param, categories-config has nameEn on all 26 categories and labelEn on all 6 EXPRESS_QUESTIONS, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire locale through API route and make engine assumptions bilingual</name>
  <files>src/app/api/chat/route.ts, src/lib/estimate/engine.ts, src/lib/ai/tools.ts</files>
  <read_first>
    - src/app/api/chat/route.ts (full file)
    - src/lib/estimate/engine.ts (full file)
    - src/lib/ai/tools.ts (full file)
    - src/lib/i18n/types.ts
    - src/lib/i18n/translations.ts
    - src/lib/estimate/__tests__/engine.test.ts
  </read_first>
  <behavior>
    - Test 1: API route reads `locale` from request body JSON and passes to buildSystemPrompt
    - Test 2: API route defaults to 'en' when no locale in request body
    - Test 3: computeEstimate with locale='es' returns assumptions with Spanish labels
    - Test 4: computeEstimate with locale='en' returns assumptions with English labels
    - Test 5: computeEstimate without locale defaults to 'en' labels
  </behavior>
  <action>
    1. Update `src/lib/estimate/engine.ts`:
       - Import `Locale`, `DEFAULT_LOCALE` from `@/lib/i18n/types`
       - Import `translations` from `@/lib/i18n/translations`
       - Change `collectAssumptions(inputs)` to `collectAssumptions(inputs, locale: Locale = 'en')`
       - Replace hardcoded Spanish labels with translations lookups:
         - `"Altura de piso a techo"` -> `translations[locale]['engine.assumptionCeilingHeight']`
         - `"Superficie de pisada"` -> `translations[locale]['engine.assumptionFootprint']`
         - `"Perimetro"` -> `translations[locale]['engine.assumptionPerimeter']`
         - `"Cantidad de puertas"` -> `translations[locale]['engine.assumptionDoors']`
         - `"Cantidad de ventanas"` -> `translations[locale]['engine.assumptionWindows']`
         - `"Cantidad de banos"` -> `translations[locale]['engine.assumptionBathrooms']`
         - `"no especificado"` -> `translations[locale]['engine.assumptionNotSpecified']`
         - `"estimado de planta cuadrada"` -> `translations[locale]['engine.assumptionEstimatedSquare']`
         - `"total / plantas"` -> `translations[locale]['engine.assumptionTotalDivStories']`
       - Change `computeEstimate(inputs)` to `computeEstimate(inputs, locale: Locale = 'en')`
       - Pass locale through to collectAssumptions
       - Also use locale for `sumByCategory` category name resolution: use `nameEn` when locale is 'en'

    2. Update `src/lib/ai/tools.ts`:
       - Tool descriptions stay in English (model-facing, not user-facing)
       - The `.describe()` strings on Zod fields stay in English (model-facing)
       - The user-facing message strings in tool execute() functions: make locale-aware
       - Import Locale type; tool execute functions that return user-facing messages should accept locale

    3. Update `src/app/api/chat/route.ts`:
       - Parse `locale` from request body: `const { messages, locale: rawLocale } = body;`
       - Validate: `const locale: Locale = (rawLocale === 'es') ? 'es' : 'en';`
       - Pass to buildSystemPrompt: `buildSystemPrompt(userMode, locale)`
       - Pass locale in tool context or closure so tools can use it for user-facing messages

    4. Update existing engine tests to account for the new optional `locale` parameter (ensure backward compat).
  </action>
  <verify>
    <automated>cd /Users/nico/dev/arqui && npx vitest run src/lib/estimate/__tests__/ && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "locale" src/app/api/chat/route.ts
    - grep -q "Locale" src/lib/estimate/engine.ts
    - grep -q "translations" src/lib/estimate/engine.ts
    - grep -q "locale" src/lib/ai/tools.ts
    - All existing 77+ tests still pass
    - New engine locale tests pass
  </acceptance_criteria>
  <done>API route accepts locale param and passes to system prompt builder, engine assumption labels are bilingual, all tests pass</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass (existing 77 + new)
- `grep -r "locale" src/app/api/chat/route.ts` shows locale handling
- `grep -r "nameEn" src/lib/pricing/categories-config.ts | wc -l` shows 26 entries
</verification>

<success_criteria>
- System prompt has complete English and Spanish versions
- All 26 categories have nameEn field
- All 6 EXPRESS_QUESTIONS have labelEn field
- Engine assumptions use translated labels based on locale
- API route reads locale from request body
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-bilingual-i18n-english-default-with-spanish-english-language-toggle/07-02-SUMMARY.md`
</output>
