---
phase: 07-bilingual-i18n
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/app/layout.tsx
  - src/components/header.tsx
  - src/components/sidebar.tsx
  - src/components/mobile-nav.tsx
  - src/components/chat-input.tsx
  - src/components/cost-breakdown.tsx
  - src/components/floor-plan-panel.tsx
  - src/app/page.tsx
  - src/app/chat/page.tsx
  - src/app/error.tsx
  - src/app/not-found.tsx
autonomous: true
requirements:
  - I18N-07
  - I18N-08

must_haves:
  truths:
    - "All hardcoded English strings in UI components use t() from useLocale"
    - "Layout wraps children in LocaleProvider"
    - "Header shows EN/ES language toggle"
    - "Switching locale updates all visible UI strings"
    - "html lang attribute reflects current locale"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Root layout with LocaleProvider wrapper"
      contains: "LocaleProvider"
    - path: "src/components/header.tsx"
      provides: "Header with language toggle"
      contains: "useLocale"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/i18n/context.tsx"
      via: "wraps children in LocaleProvider"
      pattern: "LocaleProvider"
    - from: "src/components/header.tsx"
      to: "src/lib/i18n/use-locale.ts"
      via: "useLocale hook for toggle + translations"
      pattern: "useLocale"
---

<objective>
Wire all UI components to use the i18n system: wrap the app in LocaleProvider, add a language toggle to the header, replace all hardcoded strings with t() calls, and make html lang dynamic.

Purpose: Users see the entire UI in their selected language.
Output: All 11 UI files updated to consume i18n, language toggle visible in header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-bilingual-i18n-english-default-with-spanish-english-language-toggle/07-01-SUMMARY.md

<interfaces>
From src/lib/i18n/use-locale.ts (created in plan 01):
```typescript
export function useLocale(): {
  locale: Locale;
  setLocale: (locale: Locale) => void;
  t: (key: string) => string;
};
```

From src/lib/i18n/context.tsx (created in plan 01):
```typescript
export function LocaleProvider({ children }: { children: React.ReactNode }): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap app in LocaleProvider and add language toggle to header</name>
  <files>src/app/layout.tsx, src/components/header.tsx, src/components/__tests__/header.test.tsx</files>
  <read_first>
    - src/app/layout.tsx
    - src/components/header.tsx
    - src/components/__tests__/header.test.tsx
    - src/lib/i18n/context.tsx
    - src/lib/i18n/use-locale.ts
  </read_first>
  <action>
    1. Update `src/app/layout.tsx`:
       - Convert to a Client Component layout wrapper pattern:
         - Create a `ClientProviders` component ("use client") that wraps children in `LocaleProvider`
         - The RootLayout stays as a Server Component but renders `<ClientProviders>{children}</ClientProviders>`
         - OR make the entire layout a client component with "use client" (simpler for hackathon)
       - For the `html lang` attribute: since it needs to be dynamic based on client state, the simplest approach is to use a useEffect in a client component that does `document.documentElement.lang = locale` when locale changes. Add this to the LocaleProvider itself or to a small wrapper.
       - Keep existing metadata export (metadata only works in Server Components, so if making layout a client component, move metadata to a separate `metadata.ts` or use `generateMetadata`)
       - Simplest approach: Keep layout as Server Component with `lang="en"` default. Add a `useEffect` in LocaleProvider that sets `document.documentElement.lang = locale`.

    2. Create a client wrapper component inline in layout or as separate file:
       ```tsx
       // In layout.tsx or a new src/components/providers.tsx
       "use client";
       import { LocaleProvider } from "@/lib/i18n/context";
       export function Providers({ children }: { children: React.ReactNode }) {
         return <LocaleProvider>{children}</LocaleProvider>;
       }
       ```
       Then in RootLayout body: `<Providers>{children}</Providers>`

    3. Update `src/components/header.tsx`:
       - Add "use client" directive (it currently has none — check if it needs it)
       - Import `useLocale` from `@/lib/i18n/use-locale`
       - Replace `"Base prices: Jul 2024"` with `t('header.basePrices')`
       - Replace `"New Estimate"` with `t('header.newEstimate')`
       - Add language toggle button between the base prices text and New Estimate button:
         ```tsx
         <button
           onClick={() => setLocale(locale === 'en' ? 'es' : 'en')}
           className="hidden md:flex items-center gap-1 px-3 py-1.5 rounded-full border border-outline/20 text-[10px] font-bold uppercase tracking-widest text-on-surface/60 hover:text-on-surface hover:border-outline/40 transition-all"
           aria-label={locale === 'en' ? 'Switch to Spanish' : 'Cambiar a ingles'}
         >
           <span className={locale === 'en' ? 'text-on-surface' : 'text-on-surface/40'}>EN</span>
           <span className="text-on-surface/20">/</span>
           <span className={locale === 'es' ? 'text-on-surface' : 'text-on-surface/40'}>ES</span>
         </button>
         ```

    4. Update `src/components/__tests__/header.test.tsx`:
       - Wrap test renders in LocaleProvider
       - Test that "Base prices: Jul 2024" renders (English default)
       - Test that EN/ES toggle button exists
  </action>
  <verify>
    <automated>cd /Users/nico/dev/arqui && npx vitest run src/components/__tests__/header.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "LocaleProvider" src/app/layout.tsx
    - grep -q "useLocale" src/components/header.tsx
    - grep -q "setLocale" src/components/header.tsx
    - grep -q "t(" src/components/header.tsx
    - grep -q "EN" src/components/header.tsx
    - grep -q "ES" src/components/header.tsx
    - Header test passes
  </acceptance_criteria>
  <done>App wrapped in LocaleProvider, header shows EN/ES toggle, header strings use t(), html lang updates dynamically</done>
</task>

<task type="auto">
  <name>Task 2: Convert all remaining UI components to use t() translations</name>
  <files>src/components/sidebar.tsx, src/components/mobile-nav.tsx, src/components/chat-input.tsx, src/components/cost-breakdown.tsx, src/components/floor-plan-panel.tsx, src/app/page.tsx, src/app/chat/page.tsx, src/app/error.tsx, src/app/not-found.tsx</files>
  <read_first>
    - src/components/sidebar.tsx
    - src/components/mobile-nav.tsx
    - src/components/chat-input.tsx
    - src/components/cost-breakdown.tsx
    - src/components/floor-plan-panel.tsx
    - src/app/page.tsx
    - src/app/chat/page.tsx
    - src/app/error.tsx
    - src/app/not-found.tsx
    - src/lib/i18n/translations.ts
    - src/components/__tests__/sidebar.test.tsx
    - src/components/__tests__/cost-breakdown.test.tsx
  </read_first>
  <action>
    For EVERY component listed, import `useLocale` and replace hardcoded strings with `t()` calls. Specific changes:

    **sidebar.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace navItems labels: use `t('sidebar.dashboard')`, `t('sidebar.estimates')`, `t('sidebar.blueprints')`, `t('sidebar.settings')`
    - Replace "Project Architect" with `t('sidebar.brandSubtitle')`
    - Replace "Support" with `t('sidebar.support')`
    - Replace "Soon" badge text with `t('sidebar.soon')`
    - Keep navItems as data but resolve labels via t() in the render

    **mobile-nav.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace tab labels: `t('mobileNav.chat')`, `t('mobileNav.estimates')`, `t('mobileNav.blueprints')`, `t('mobileNav.profile')`

    **chat-input.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace default placeholder: `t('chatInput.placeholder')`
    - Replace "Analyze this floor plan" fallback: `t('chatInput.analyzeFloorPlan')`
    - Replace disclaimer text: `t('chatInput.disclaimer')`

    **cost-breakdown.tsx:**
    - Add "use client" (check if already present)
    - `const { t } = useLocale();`
    - Replace "Estimated Budget" with `t('costBreakdown.estimatedBudget')`
    - Replace "Price per m2" with `t('costBreakdown.pricePerM2')`
    - Replace "Confidence Level" with `t('costBreakdown.confidenceLevel')`
    - Replace "Category" with `t('costBreakdown.category')`
    - Replace "Incidence" with `t('costBreakdown.incidence')`
    - Replace "Cost" with `t('costBreakdown.cost')`
    - Replace "Direct Cost" with `t('costBreakdown.directCost')`
    - Replace "Overhead" with `t('costBreakdown.overhead')`
    - Replace "Profit" with `t('costBreakdown.profit')`
    - Replace "Subtotal" with `t('costBreakdown.subtotal')`
    - Replace "IVA (21%)" with `t('costBreakdown.iva')`
    - Replace "Total Price" with `t('costBreakdown.totalPrice')`
    - Replace "Assumptions" with `t('costBreakdown.assumptions')`
    - Replace confidenceLabels map: use `t('costBreakdown.confidenceQuick')` etc.

    **floor-plan-panel.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace "Analysis Phase" with `t('floorPlanPanel.analysisPhase')`
    - Replace "Floor Plan Analysis Complete" with `t('floorPlanPanel.analysisComplete')`
    - Replace field labels: totalArea, rooms, bathrooms, windows
    - Replace "Confirm and Calculate" with `t('floorPlanPanel.confirm')`

    **page.tsx (landing):**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace "Hi, I'm Nelo" with `t('landing.greeting')`
    - Replace "What do you want to build today?" with `t('landing.subtitle')`
    - Replace quickPrompts texts with `t('landing.quickPrompt1')`, `t('landing.quickPrompt2')`
    - Replace proPrompts texts with `t('landing.proPrompt1')`, `t('landing.proPrompt2')`
    - Replace section headers with `t('landing.sectionQuick')`, `t('landing.sectionPro')`

    **chat/page.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace empty state title/subtitle with `t('chat.emptyTitle')`, `t('chat.emptySubtitle')`
    - Replace error message with `t('chat.errorMessage')`
    - Replace prompt suggestions with `t('chat.promptSuggestion1')`, `t('chat.promptSuggestion2')`
    - IMPORTANT: Pass locale to the API — update the transport or sendMessage to include locale in the request body. Use `useMemo` to create transport with locale in body, or modify the fetch call.

    **error.tsx:**
    - Already "use client"
    - `const { t } = useLocale();`
    - Replace "Something went wrong" with `t('error.title')`
    - Replace description with `t('error.description')`
    - Replace "Try Again" with `t('error.tryAgain')`

    **not-found.tsx:**
    - This is a Server Component currently. Convert to Client Component with "use client".
    - `const { t } = useLocale();`
    - Replace "404" heading (keep as "404")
    - Replace description with `t('notFound.description')`
    - Replace "Back to Home" with `t('notFound.backHome')`

    Update ALL component tests to wrap renders in `<LocaleProvider>`. This is critical — tests will fail without the provider.
  </action>
  <verify>
    <automated>cd /Users/nico/dev/arqui && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "useLocale" src/components/sidebar.tsx
    - grep -q "useLocale" src/components/mobile-nav.tsx
    - grep -q "useLocale" src/components/chat-input.tsx
    - grep -q "useLocale" src/components/cost-breakdown.tsx
    - grep -q "useLocale" src/components/floor-plan-panel.tsx
    - grep -q "useLocale" src/app/page.tsx
    - grep -q "useLocale" src/app/chat/page.tsx
    - grep -q "useLocale" src/app/error.tsx
    - grep -q "useLocale" src/app/not-found.tsx
    - grep -rL "useLocale" src/components/sidebar.tsx src/components/mobile-nav.tsx src/components/chat-input.tsx | wc -l returns 0
    - All tests pass (existing + new)
  </acceptance_criteria>
  <done>Every UI component uses t() for user-visible strings, no hardcoded English/Spanish text remains in UI files, all tests pass, chat page sends locale to API</done>
</task>

</tasks>

<verification>
- `npx vitest run` — all tests pass
- `grep -rn "useLocale" src/components/ src/app/` — shows all components using the hook
- No hardcoded user-visible English strings remain (except brand name "Nelo" which is not translated)
</verification>

<success_criteria>
- All 11 UI files use t() for every user-visible string
- Header has functional EN/ES toggle button
- App is wrapped in LocaleProvider
- html lang attribute updates with locale
- Chat page sends locale to API route
- All tests pass with LocaleProvider wrappers
</success_criteria>

<output>
After completion, create `.planning/phases/07-bilingual-i18n-english-default-with-spanish-english-language-toggle/07-03-SUMMARY.md`
</output>
